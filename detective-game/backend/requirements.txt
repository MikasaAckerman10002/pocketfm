# Core — required.
fastapi>=0.115
uvicorn[standard]>=0.32
pydantic>=2.9
python-dotenv>=1.0
httpx>=0.27

# Optional. Only needed if you point TEXT_PROVIDER or IMAGE_PROVIDER at Gemini.
# The default setup (Groq for text, OpenRouter for hotspot detection, Nano Banana for
# art, ElevenLabs for voice) never imports it, and the app starts fine without it.
google-genai>=2.0
